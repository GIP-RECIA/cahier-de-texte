/**
 * Fonction qui va appliquer une rotation de 45° sur les div .tamponImage
 */
function appliqueRotationTampon() {
    var divDate = jQuery('.tamponImage');
    if (divDate != null) {
      /*  divDate.jangle(315);
        divDate.css({
            position: "static"
        });
       */
    }
}